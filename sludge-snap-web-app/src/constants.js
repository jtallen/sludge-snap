// declare a set of functions that can do translations
// get python value from app value
// get database value from app value
// maybe make a new file like "values map" or something
// one place that exports an API that takes in values and transforms them to targets based on src and destination
// upper case definition of values = "turner value"
// TODO: above

const valuesMap = {};

export { valuesMap };
